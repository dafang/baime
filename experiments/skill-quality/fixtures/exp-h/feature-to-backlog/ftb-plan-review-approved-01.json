{
  "id": "ftb-plan-review-approved-01",
  "skill": "feature-to-backlog",
  "taskClass": "B",
  "taskType": "invariant-check",
  "decisionPoint": "reviewPlan",
  "specSection": "reviewPlan :: (Plan, Config) → Verdict\nreviewPlan(P, cfg) = {\n  ∀phase ∈ P.phases: {\n    assert: ¬empty(phase.tests),\n    assert: phase.dod[0] starts with cfg.testCmd,\n    assert: ∀cmd ∈ phase.dod: isShellCmd(cmd)\n  },\n  assert: P.acceptance[0] == cfg.testAll,\n  assert: ∀goal ∈ proposal.goals: coveredBy(goal, P.phases ∪ P.acceptance),\n  return: APPROVED | NEEDS_REVISION\n}",
  "config": {
    "testCmd": "npm test -- --run",
    "testAll": "npm test"
  },
  "plan": {
    "phases": [
      {
        "title": "Implement OAuth2 login",
        "tests": ["src/auth/__tests__/oauth2.test.ts"],
        "impl": ["src/auth/oauth2.ts", "src/routes/auth.ts"],
        "dod": ["npm test -- --run src/auth/__tests__/oauth2.test.ts", "npm run lint"]
      }
    ],
    "constraints": ["Must not break existing session handling"],
    "acceptance": ["npm test", "curl -s http://localhost:3000/health | grep OK"]
  },
  "ground_truth_rationale": "All invariants pass: phase has tests (non-empty), dod[0] starts with cfg.testCmd ('npm test -- --run'), all dod items are shell commands, acceptance[0] == cfg.testAll ('npm test'). No issues found.",
  "answer": {
    "verdict": "APPROVED",
    "failing_invariants": []
  },
  "answerType": "partial",
  "fixtureClass": "CLEAR"
}
